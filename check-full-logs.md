# 🔍 Проверка полных логов

## Проверьте больше строк логов:

```bash
pm2 logs colizeum-game --lines 30
```

Ищите строку:
- `🎮 Game available at https://osiptzovs.fvds.ru` ✅

## Если строка не видна, проверьте .env файл:

```bash
cat .env | grep FRONTEND_URL
```

Должно быть:
```
FRONTEND_URL=https://osiptzovs.fvds.ru
```

(БЕЗ слэша в конце!)

## Проверьте, что переменные окружения загружены:

```bash
cd /var/www/colizeum-game/server
node -e "require('dotenv').config({path: '../.env'}); console.log('FRONTEND_URL:', process.env.FRONTEND_URL)"
```

Должно вывести:
```
FRONTEND_URL: https://osiptzovs.fvds.ru
```

## Если все правильно, но бот все еще показывает старый URL:

1. Проверьте, что вы обновили .env файл
2. Перезапустите с --update-env:
   ```bash
   pm2 restart colizeum-game --update-env
   ```
3. Проверьте логи снова
4. Попробуйте отправить `/start` боту заново

## Проверка в Telegram:

1. Откройте бота
2. Отправьте `/start`
3. Нажмите "🎮 Начать игру"
4. Проверьте URL в адресной строке браузера - должен быть `https://osiptzovs.fvds.ru`


