# 🚀 Деплой Colizeum Tower Game на VDS

Этот проект готов к деплою на VDS (Virtual Dedicated Server).

## 📋 Что включено

- ✅ Конфигурация Nginx для фронтенда и API
- ✅ PM2 конфигурация для управления процессом
- ✅ Скрипт автоматического деплоя
- ✅ Поддержка SSL (Let's Encrypt)
- ✅ Резервное копирование базы данных
- ✅ Полная документация

## 🎯 Быстрый старт

1. **Прочитайте** `QUICK_VDS_DEPLOY.md` для быстрой установки (5 минут)
2. **Или** `VDS_DEPLOY.md` для детальной инструкции

## 📁 Структура файлов для деплоя

```
├── VDS_DEPLOY.md          # Детальная инструкция по деплою
├── QUICK_VDS_DEPLOY.md    # Быстрая инструкция (5 минут)
├── deploy.sh              # Скрипт автоматического деплоя
├── ecosystem.config.js    # PM2 конфигурация
├── nginx.conf             # Nginx конфигурация
└── .env.example           # Пример переменных окружения
```

## 🔧 Основные шаги

1. **Установите зависимости** на сервере (Node.js, Nginx, PM2)
2. **Загрузите проект** на сервер (Git или SCP)
3. **Настройте .env** файл с вашими токенами
4. **Настройте Nginx** (скопируйте nginx.conf)
5. **Настройте SSL** (Let's Encrypt)
6. **Запустите** через PM2

## 📝 Переменные окружения

Создайте файл `.env` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_WEBHOOK_URL=https://ваш-домен.com/webhook
JWT_SECRET=случайная_строка_32_символа
DATABASE_PATH=./server/database/game.db
PORT=3000
FRONTEND_URL=https://ваш-домен.com
```

## 🛠️ Команды для управления

```bash
# Деплой
./deploy.sh

# PM2 команды
pm2 status              # Статус приложения
pm2 logs colizeum-game   # Логи
pm2 restart colizeum-game # Перезапуск
pm2 stop colizeum-game   # Остановка

# Обновление проекта
git pull
./deploy.sh
```

## 🔒 Безопасность

- ✅ SSL сертификаты (Let's Encrypt)
- ✅ Защита от прямого доступа к служебным файлам
- ✅ Безопасные заголовки в Nginx
- ✅ JWT аутентификация

## 📊 Мониторинг

- PM2 мониторинг: `pm2 monit`
- Логи: `pm2 logs colizeum-game`
- Nginx логи: `/var/log/nginx/colizeum-game-error.log`

## 💾 Резервное копирование

База данных автоматически бэкапится (настройте в crontab):
```bash
0 3 * * * /var/www/colizeum-game/backup-db.sh
```

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи PM2: `pm2 logs`
2. Проверьте логи Nginx: `tail -f /var/log/nginx/error.log`
3. Проверьте статус сервисов: `systemctl status nginx`

## 📚 Дополнительная документация

- `VDS_DEPLOY.md` - Полная инструкция
- `QUICK_VDS_DEPLOY.md` - Быстрая установка
- `README.md` - Общая информация о проекте


