# 🔧 Исправление ошибки деплоя на Render

## ❌ Ошибка: "cross-env: not found"

Эта ошибка означает, что `cross-env` не установлен или не найден. Проблема в скрипте `build` в `package.json`.

## ✅ Решение: Убрать cross-env из скрипта build

**Исправлено в package.json:** Скрипт `build` теперь не использует `cross-env`.

## 📋 Обновить Build Command в Render

### В настройках Render измените Build Command на:

```bash
npm install && cd server && npm install && cd .. && NODE_OPTIONS=--openssl-legacy-provider npm run build
```

**Или более простой вариант (без сборки frontend):**

```bash
npm install && cd server && npm install
```

### Start Command должен быть:

```bash
cd server && node server.js
```

## 📋 Пошаговая инструкция:

1. Откройте ваш сервис на Render: https://dashboard.render.com
2. Нажмите на сервис "colizeum-tower-game"
3. Перейдите в **"Settings"** (Настройки)
4. Найдите секцию **"Build & Deploy"**
5. Найдите поле **"Build Command"**
6. Замените команду на:
   ```
   npm install && cd server && npm install && cd .. && NODE_OPTIONS=--openssl-legacy-provider npm run build
   ```
7. Убедитесь, что **"Start Command"** равен:
   ```
   cd server && node server.js
   ```
8. Нажмите **"Save Changes"**

## 🔄 Альтернативный вариант (без сборки frontend)

Если сборка frontend вызывает проблемы, можно использовать упрощенную версию:

**Build Command:**
```bash
npm install && cd server && npm install
```

**Start Command:**
```bash
cd server && node server.js
```

В этом случае frontend будет работать, но без оптимизации webpack. Для production лучше использовать полную сборку.

## ⚠️ Важно:

- Убедитесь, что все переменные окружения установлены
- После изменения команд Render автоматически запустит новый деплой
- Проверьте логи деплоя после сохранения

## 🔍 Проверка логов:

1. После сохранения перейдите в **"Logs"** (Логи)
2. Следите за процессом сборки
3. Если ошибка повторится, скопируйте логи и проверьте, какая именно команда не работает

