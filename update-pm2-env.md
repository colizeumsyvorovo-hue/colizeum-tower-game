# 🔄 Обновление переменных окружения в PM2

## Проблема:
PM2 показывает предупреждение: "Use --update-env to update environment variables"

Это значит, что PM2 не подхватил изменения в .env файле.

## Решение:

### Вариант 1: Перезапуск с обновлением env (рекомендуется)

```bash
pm2 restart colizeum-game --update-env
```

### Вариант 2: Полная перезагрузка

```bash
pm2 delete colizeum-game
pm2 start ecosystem.config.js
pm2 save
```

### Вариант 3: Использование скрипта деплоя

```bash
./deploy.sh
```

## После перезапуска проверьте:

```bash
pm2 logs colizeum-game --lines 15
```

Должно быть:
- `🎮 Game available at https://osiptzovs.fvds.ru` (без слэша в конце) ✅
- `🔧 Setting webhook to: https://osiptzovs.fvds.ru/webhook` ✅

## Проверка в Telegram:

1. Отправьте `/start` боту
2. Нажмите "🎮 Начать игру"
3. Игра должна открыться на `https://osiptzovs.fvds.ru`

