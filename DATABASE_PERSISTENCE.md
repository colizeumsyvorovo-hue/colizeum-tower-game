# 💾 Сохранение базы данных при пересборке

## ✅ Гарантии сохранения данных

База данных **НЕ удаляется** при пересборке проекта, потому что:

1. **База данных находится отдельно от кода сборки:**
   - Путь: `server/database/game.db`
   - Сборка фронтенда (`npm run build`) создает только `dist/main.js`
   - База данных не затрагивается процессом сборки

2. **База данных в .gitignore:**
   - Файл `.gitignore` исключает `*.db` и `server/database/*.db`
   - База данных не попадает в репозиторий
   - Каждый сервер имеет свою собственную базу данных

3. **Автоматическое создание директории:**
   - При запуске сервера автоматически создается директория `server/database/`, если её нет
   - Код в `server/database.js` проверяет и создает директорию

## 🔒 Что сохраняется в базе данных:

- ✅ Все пользователи и их данные
- ✅ Накопительный банк бонусов (`total_bonuses`)
- ✅ Статистика игр (`total_games`, `best_score`)
- ✅ История всех игр
- ✅ Попытки игры за бонусы и таймеры

## 📋 Процесс обновления:

```bash
# 1. Получить обновления из Git
git pull origin main

# 2. Пересобрать фронтенд (база данных НЕ затрагивается)
npm run build

# 3. Перезапустить сервер
pm2 restart colizeum-game --update-env
```

**База данных остается нетронутой!** ✅

## 🛡️ Резервное копирование (рекомендуется):

Создайте скрипт для бэкапа:

```bash
#!/bin/bash
# backup-db.sh
BACKUP_DIR="/var/backups/colizeum-game"
mkdir -p $BACKUP_DIR
cp /var/www/colizeum-game/server/database/game.db $BACKUP_DIR/game_$(date +%Y%m%d_%H%M%S).db
find $BACKUP_DIR -name "game_*.db" -mtime +7 -delete
```

Добавьте в crontab для ежедневного бэкапа:
```bash
0 3 * * * /var/www/colizeum-game/backup-db.sh
```

