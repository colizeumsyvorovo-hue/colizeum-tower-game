# 🔧 Исправление URL игры в Telegram боте

## Проблема:
В боте кнопка "Начать игру" все еще ведет на старый URL (Render.com), а не на новый VDS.

## Причина:
В `.env` файле `FRONTEND_URL` все еще указывает на старый домен.

## Решение:

### Шаг 1: Обновите .env файл

```bash
cd /var/www/colizeum-game
nano .env
```

Убедитесь, что `FRONTEND_URL` указывает на новый домен:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_WEBHOOK_URL=https://osiptzovs.fvds.ru
JWT_SECRET=ваш_jwt_secret
DATABASE_PATH=./server/database/game.db
PORT=3000
FRONTEND_URL=https://osiptzovs.fvds.ru
```

**Важно:** `FRONTEND_URL` должен быть `https://osiptzovs.fvds.ru` (не старый URL с Render.com!)

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 2: Перезапустите приложение

```bash
pm2 restart colizeum-game
```

### Шаг 3: Проверьте логи

```bash
pm2 logs colizeum-game --lines 10
```

Должно быть:
- `🎮 Game available at https://osiptzovs.fvds.ru/` ✅

### Шаг 4: Проверьте в Telegram

1. Отправьте `/start` боту
2. Нажмите на кнопку "🎮 Начать игру"
3. Игра должна открыться на `https://osiptzovs.fvds.ru`

## Если не помогло:

Проверьте, что в .env файле нет старого URL:

```bash
cat .env | grep FRONTEND_URL
```

Должно быть:
```
FRONTEND_URL=https://osiptzovs.fvds.ru
```

Если там старый URL - исправьте и перезапустите снова.


