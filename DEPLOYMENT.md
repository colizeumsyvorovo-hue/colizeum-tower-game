# 🚀 Варианты деплоя проекта

## Проблема с GitHub Pages

GitHub Pages - это **статический хостинг**, который может размещать только HTML/CSS/JavaScript файлы. Он **не может** запускать:
- ❌ Node.js сервер
- ❌ Базу данных
- ❌ Telegram бота
- ❌ API endpoints

## ✅ Решения

### Вариант 1: GitHub Pages (Frontend) + Отдельный хостинг (Backend)

**Frontend на GitHub Pages:**
- Бесплатно
- Автоматический деплой при push
- HTTPS включен

**Backend на бесплатном хостинге:**
- Railway.app (бесплатный tier)
- Render.com (бесплатный tier)
- Fly.io (бесплатный tier)
- Heroku (платный, но есть альтернативы)

**Как это работает:**
1. Frontend (HTML/CSS/JS) → GitHub Pages
2. Backend (Node.js сервер) → Отдельный хостинг
3. Frontend делает запросы к API на backend хостинге

### Вариант 2: Полный деплой на один хостинг

**Варианты:**
- **Railway.app** - простой деплой, бесплатный tier
- **Render.com** - бесплатный tier, автоматический деплой из GitHub
- **Fly.io** - бесплатный tier
- **VPS** (DigitalOcean, Vultr) - от $5/месяц, полный контроль

### Вариант 3: Serverless (Vercel/Netlify)

- Frontend → Vercel/Netlify
- Backend → Serverless Functions (Vercel/Netlify)
- База данных → SQLite в файловой системе или внешняя БД

## 🎯 Рекомендуемый вариант: Render.com

**Почему Render:**
- ✅ Бесплатный tier
- ✅ Автоматический деплой из GitHub
- ✅ Поддержка Node.js
- ✅ PostgreSQL (можно использовать вместо SQLite)
- ✅ Webhooks для Telegram бота

**Инструкция по деплою на Render:**

1. Зайдите на https://render.com
2. Подключите GitHub аккаунт
3. Создайте новый "Web Service"
4. Выберите репозиторий `colizeum-tower-game`
5. Настройки:
   - **Build Command:** `npm install && cd server && npm install && cd .. && npm run build`
   - **Start Command:** `cd server && node server.js`
   - **Environment Variables:**
     - `PORT=3000`
     - `TELEGRAM_BOT_TOKEN=your_token`
     - `JWT_SECRET=your_secret`
     - `DATABASE_PATH=./database/game.db`
     - `FRONTEND_URL=https://your-app.onrender.com`
6. Нажмите "Create Web Service"

## 📝 Настройка для GitHub Pages + Backend

Если хотите использовать GitHub Pages для фронтенда:

1. **Измените API_BASE в index.html:**
```javascript
const API_BASE = 'https://your-backend.onrender.com';
```

2. **Создайте папку `docs/` или используйте `gh-pages` ветку**

3. **Настройте GitHub Pages:**
   - Settings → Pages
   - Source: `main` branch, folder: `/` (root)

4. **Backend на Render/Railway:**
   - Деплой сервера отдельно
   - Настройте CORS для разрешения запросов с GitHub Pages

## 🔧 Быстрый старт с Render

1. Форкните или используйте ваш репозиторий
2. На Render создайте Web Service
3. Добавьте переменные окружения
4. Деплой автоматически запустится

## ⚠️ Важно

- На бесплатном tier сервер может "засыпать" после неактивности
- Первый запрос может быть медленным (cold start)
- Для production лучше использовать платный tier

