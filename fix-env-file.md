# 🔧 Исправление .env файла

## Проблемы в текущем .env:

1. `FRONTEND_URL=https://osiptzovs.fvds.ru/` - есть слэш в конце (нужно убрать)
2. `JWT_SECRET=colizeum-secret-key-change-in-production` - используется дефолтное значение (небезопасно!)

## Исправленный .env:

```env
# Токен Telegram бота (получить у @BotFather)
TELEGRAM_BOT_TOKEN=8370248936:AAF2RVEr0eBP0wnLeBB1-LQ17bqti_qFXhM

# URL для Telegram Webhook (опционально)
TELEGRAM_WEBHOOK_URL=https://osiptzovs.fvds.ru

# Секретный ключ для JWT (сгенерируйте новый!)
JWT_SECRET=ВАШ_НОВЫЙ_СЛУЧАЙНЫЙ_КЛЮЧ_32_СИМВОЛА

# Путь к базе данных
DATABASE_PATH=./server/database/game.db

# URL фронтенда (БЕЗ слэша в конце!)
FRONTEND_URL=https://osiptzovs.fvds.ru
```

## Что нужно сделать:

1. **Убрать слэш** из конца `FRONTEND_URL`
2. **Сгенерировать новый JWT_SECRET** для безопасности

## Команды для исправления:

```bash
cd /var/www/colizeum-game

# Сгенерируйте новый JWT_SECRET
openssl rand -base64 32

# Скопируйте результат и обновите .env
nano .env
```

Исправьте:
- `FRONTEND_URL=https://osiptzovs.fvds.ru` (уберите `/` в конце)
- `JWT_SECRET=...` (вставьте сгенерированный ключ)

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

## Перезапустите приложение:

```bash
pm2 restart colizeum-game
```

## Проверьте:

```bash
pm2 logs colizeum-game --lines 10
```

Должно быть:
- `🎮 Game available at https://osiptzovs.fvds.ru` (без слэша в конце)

