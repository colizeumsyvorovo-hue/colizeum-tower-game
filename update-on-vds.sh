#!/bin/bash

# Скрипт для обновления проекта на VDS
# Использование: ./update-on-vds.sh

set -e

echo "🔄 Обновление проекта на VDS..."

# Цвета для вывода
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m' # No Color

# Проверка, что мы в правильной директории
if [ ! -f "package.json" ]; then
    echo -e "${RED}❌ Ошибка: package.json не найден. Убедитесь, что вы в корне проекта.${NC}"
    exit 1
fi

echo -e "${YELLOW}📥 Получение обновлений из Git...${NC}"
git pull origin main

echo -e "${YELLOW}📦 Обновление зависимостей (если нужно)...${NC}"
npm install

echo -e "${YELLOW}📦 Обновление зависимостей сервера...${NC}"
cd server
npm install
cd ..

echo -e "${YELLOW}🔨 Пересборка фронтенда...${NC}"
npm run build

echo -e "${YELLOW}🔄 Перезапуск приложения через PM2...${NC}"
pm2 restart colizeum-game --update-env

echo -e "${GREEN}✅ Обновление завершено успешно!${NC}"
echo -e "${GREEN}📊 Статус приложения:${NC}"
pm2 status

echo -e "${GREEN}📝 Логи можно посмотреть командой: pm2 logs colizeum-game${NC}"

