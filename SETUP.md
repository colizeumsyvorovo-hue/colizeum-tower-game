# 🚀 Быстрая настройка Colizeum Tower Game

## Шаг 1: Установка зависимостей

```bash
cd server
npm install
```

## Шаг 2: Создание Telegram бота

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте команду `/newbot`
4. Следуйте инструкциям:
   - Введите имя бота (например: `Colizeum Tower Game`)
   - Введите username бота (например: `colizeum_tower_bot`)
5. Скопируйте полученный токен (выглядит как: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 3: Настройка .env файла

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
PORT=3000
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
JWT_SECRET=colizeum-secret-key-change-in-production
DATABASE_PATH=./database/game.db
FRONTEND_URL=http://localhost:3000
```

**Важно:** Замените `ваш_токен_от_BotFather` на токен, полученный от BotFather!

## Шаг 4: Запуск сервера

### Windows:
```bash
cd server
npm start
```

Или используйте `start.bat`:
```bash
cd server
start.bat
```

### Linux/Mac:
```bash
cd server
npm start
```

Или используйте `start.sh`:
```bash
cd server
chmod +x start.sh
./start.sh
```

## Шаг 5: Настройка Telegram Web App

1. Откройте вашего бота в Telegram
2. Отправьте команду `/start`
3. Нажмите на кнопку "🎮 Начать игру"

## 🎮 Готово!

Теперь игра должна работать! 

### Проверка работы:

1. Откройте бота в Telegram
2. Отправьте `/start` - должна появиться кнопка для запуска игры
3. Отправьте `/stats` - должна показаться статистика (пока пустая)
4. Отправьте `/help` - должна показаться справка

## 🔧 Настройка для продакшена

### 1. Измените JWT_SECRET

В файле `.env` замените `JWT_SECRET` на случайную строку:

```env
JWT_SECRET=ваша_случайная_строка_минимум_32_символа
```

### 2. Настройте FRONTEND_URL

Укажите реальный URL вашего сервера:

```env
FRONTEND_URL=https://yourdomain.com
```

### 3. Настройте Webhook (опционально)

Для продакшена рекомендуется настроить webhook вместо polling:

1. Установите переменную `TELEGRAM_WEBHOOK_URL` в `.env`
2. Используйте HTTPS для webhook URL

## ❓ Проблемы?

### Бот не отвечает
- Проверьте, что токен указан правильно
- Убедитесь, что сервер запущен
- Проверьте логи в консоли

### Ошибка "Invalid Telegram data"
- Убедитесь, что игра открыта через Telegram Web App
- Проверьте, что `FRONTEND_URL` указан правильно

### База данных не создается
- Проверьте права на запись в директорию
- Убедитесь, что путь к базе данных указан правильно

---

**Удачи! 🎄**



