# 📋 Следующие шаги после установки зависимостей

## ✅ Что уже сделано:
- ✅ Git установлен
- ✅ Node.js установлен
- ✅ Зависимости установлены
- ✅ Проект клонирован

## 🚀 Что делать дальше:

### Шаг 1: Соберите фронтенд

```bash
cd /var/www/colizeum-game
npm run build
```

Должно появиться сообщение о успешной сборке.

### Шаг 2: Создайте .env файл

```bash
nano .env
```

Скопируйте и вставьте (замените значения на свои):

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота_от_BotFather
TELEGRAM_WEBHOOK_URL=https://ваш-домен.com/webhook
JWT_SECRET=сгенерируйте_случайную_строку_минимум_32_символа
DATABASE_PATH=./server/database/game.db
PORT=3000
FRONTEND_URL=https://ваш-домен.com
```

**Как сгенерировать JWT_SECRET:**
```bash
openssl rand -base64 32
```

Сохраните файл: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 3: Настройте Nginx

**Вариант A: Используйте скрипт (рекомендуется)**
```bash
# Скачайте скрипт создания конфигурации (если его нет)
# Или создайте конфигурацию вручную (см. Вариант B)
```

**Вариант B: Создайте конфигурацию вручную**
```bash
# Создайте файл конфигурации
nano /etc/nginx/sites-available/colizeum-game
```

Скопируйте содержимое ниже (замените `ваш-домен.com` на ваш реальный домен):

Найдите и замените все `yourdomain.com` на ваш реальный домен (например, `game.colizeum.ru`).

Включите конфигурацию:
```bash
ln -s /etc/nginx/sites-available/colizeum-game /etc/nginx/sites-enabled/

# Проверьте конфигурацию
nginx -t

# Если все ОК, перезагрузите Nginx
systemctl reload nginx
```

### Шаг 4: Настройте SSL (Let's Encrypt)

**Важно:** Сначала нужно создать временную конфигурацию БЕЗ SSL, чтобы Nginx мог запуститься.

Если у вас уже есть конфигурация с SSL и она не работает, создайте временную:

```bash
# Создайте временную конфигурацию БЕЗ SSL
bash create-nginx-config-temp.sh

# Или вручную отредактируйте файл, убрав SSL директивы
nano /etc/nginx/sites-available/colizeum-game
```

Затем настройте SSL:

```bash
certbot --nginx -d ваш-домен.com
```

Следуйте инструкциям:
- Введите email
- Согласитесь с условиями (A)
- Выберите редирект HTTP на HTTPS (2)

Certbot автоматически обновит конфигурацию и добавит SSL.

### Шаг 5: Запустите приложение через PM2

```bash
cd /var/www/colizeum-game

# Сделайте скрипт деплоя исполняемым
chmod +x deploy.sh

# Запустите деплой
./deploy.sh
```

Или вручную:
```bash
# Создайте директорию для логов
mkdir -p logs

# Запустите через PM2
pm2 start ecosystem.config.js

# Сохраните конфигурацию PM2
pm2 save
```

### Шаг 6: Настройте автозапуск PM2

```bash
pm2 startup
```

Выполните команду, которую покажет PM2 (обычно что-то вроде `sudo env PATH=... pm2 startup systemd -u root --hp /root`).

Затем:
```bash
pm2 save
```

## ✅ Проверка работы

1. **Проверьте статус PM2:**
```bash
pm2 status
```

2. **Проверьте логи:**
```bash
pm2 logs colizeum-game
```

3. **Откройте в браузере:**
```
https://ваш-домен.com
```

4. **Проверьте Telegram бота:**
Отправьте `/start` боту в Telegram

## 🔧 Полезные команды

```bash
# Просмотр логов
pm2 logs colizeum-game

# Перезапуск приложения
pm2 restart colizeum-game

# Остановка
pm2 stop colizeum-game

# Статус
pm2 status

# Мониторинг
pm2 monit
```

## 🆘 Если что-то не работает

1. **Проверьте логи PM2:**
```bash
pm2 logs colizeum-game --lines 50
```

2. **Проверьте логи Nginx:**
```bash
tail -f /var/log/nginx/colizeum-game-error.log
```

3. **Проверьте, что порт 3000 свободен:**
```bash
netstat -tulpn | grep 3000
```

4. **Проверьте статус Nginx:**
```bash
systemctl status nginx
```

## 📝 Обновление проекта в будущем

```bash
cd /var/www/colizeum-game
git pull
./deploy.sh
```

