# 🔧 Обновление переменных в Render.com

## ✅ Ваш URL: `https://colizeum-tower-game.onrender.com`

## 📍 Где изменить переменную FRONTEND_URL:

### Шаг 1: Откройте ваш сервис на Render

1. Зайдите на https://dashboard.render.com
2. Найдите ваш сервис **"colizeum-tower-game"**
3. Нажмите на него

### Шаг 2: Откройте настройки Environment Variables

1. В меню слева или вверху найдите **"Environment"** или **"Environment Variables"**
2. Или прокрутите вниз до секции **"Environment Variables"**

### Шаг 3: Обновите FRONTEND_URL

1. Найдите переменную `FRONTEND_URL` в списке
2. Нажмите на неё (или на иконку редактирования ✏️)
3. Измените значение на:
   ```
   https://colizeum-tower-game.onrender.com
   ```
4. Нажмите **"Save"** или **"Update"**

### Шаг 4: Сохраните изменения

После изменения переменной Render автоматически перезапустит сервис с новыми настройками.

## 📋 Полный список переменных для вашего проекта:

Убедитесь, что у вас есть все эти переменные:

| NAME | Value |
|------|-------|
| `NODE_ENV` | `production` |
| `PORT` | `3000` |
| `DATABASE_PATH` | `./server/database/game.db` |
| `TELEGRAM_BOT_TOKEN` | `ваш_токен_от_BotFather` |
| `JWT_SECRET` | `ваш_секретный_ключ` |
| `FRONTEND_URL` | `https://colizeum-tower-game.onrender.com` ✅ |

## 🔗 Настройка Telegram Webhook (после обновления переменной)

После того как сервис перезапустится, настройте webhook для бота:

### Вариант 1: Через браузер

Откройте в браузере (замените `YOUR_BOT_TOKEN` на ваш токен):
```
https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook?url=https://colizeum-tower-game.onrender.com/webhook
```

### Вариант 2: Через curl (в терминале)

```bash
curl -X POST "https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook?url=https://colizeum-tower-game.onrender.com/webhook"
```

### Вариант 3: Проверка webhook

Проверьте, что webhook установлен:
```
https://api.telegram.org/botYOUR_BOT_TOKEN/getWebhookInfo
```

## ✅ Проверка работы

После обновления переменной и настройки webhook:

1. **Откройте игру:** https://colizeum-tower-game.onrender.com
2. **Проверьте бота:** Откройте бота в Telegram и отправьте `/start`
3. **Проверьте API:** Откройте https://colizeum-tower-game.onrender.com/api/stats (должна быть ошибка авторизации, но не 404)

## 🎯 Что дальше?

После обновления `FRONTEND_URL`:
- ✅ Telegram бот будет отправлять правильные ссылки на игру
- ✅ Web App кнопка будет работать корректно
- ✅ Все API запросы будут работать



