# 🚀 Быстрый деплой на Render.com (Рекомендуется)

## Почему Render.com?

✅ **Бесплатный tier** - достаточно для тестирования  
✅ **Автоматический деплой** из GitHub  
✅ **Поддержка Node.js** и базы данных  
✅ **HTTPS** включен автоматически  
✅ **Простая настройка** - 5 минут

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка репозитория

Репозиторий уже на GitHub: ✅  
Файл `render.yaml` создан: ✅

### Шаг 2: Создание аккаунта на Render

1. Перейдите на https://render.com
2. Нажмите "Get Started for Free"
3. Войдите через GitHub (рекомендуется)

### Шаг 3: Создание Web Service

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Выберите репозиторий `colizeum-tower-game`
3. Нажмите **"Connect"**

### Шаг 4: Настройка сервиса

**Основные настройки:**
- **Name:** `colizeum-tower-game` (или любое другое)
- **Region:** `Frankfurt` (ближайший к России)
- **Branch:** `main`
- **Root Directory:** (оставьте пустым)
- **Runtime:** `Node`
- **Build Command:** 
  ```bash
  npm install && cd server && npm install && cd .. && NODE_OPTIONS=--openssl-legacy-provider npm run build
  ```
- **Start Command:**
  ```bash
  cd server && node server.js
  ```

### Шаг 5: Переменные окружения

Нажмите **"Advanced"** и добавьте переменные:

```
NODE_ENV=production
PORT=3000
DATABASE_PATH=./server/database/game.db
TELEGRAM_BOT_TOKEN=ваш_токен_бота
JWT_SECRET=случайная_строка_для_безопасности
FRONTEND_URL=https://your-app-name.onrender.com
```

**Как получить TELEGRAM_BOT_TOKEN:**
1. Откройте @BotFather в Telegram
2. Отправьте `/newbot` или `/mybots`
3. Скопируйте токен

**JWT_SECRET:** Любая случайная строка (например, сгенерируйте на https://randomkeygen.com)

### Шаг 6: Деплой

1. Нажмите **"Create Web Service"**
2. Дождитесь завершения деплоя (5-10 минут)
3. После деплоя вы получите URL: `https://your-app-name.onrender.com`

### Шаг 7: Настройка Telegram Webhook

После деплоя настройте webhook для бота:

1. Откройте ваш бот в Telegram
2. Отправьте команду `/setwebhook`
3. Или используйте API:
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://your-app-name.onrender.com/webhook
   ```

## 🎯 Альтернатива: GitHub Pages + Render Backend

Если хотите использовать GitHub Pages для фронтенда:

### 1. Деплой Backend на Render (как выше)

### 2. Измените API_BASE в index.html:

```javascript
// Для production
const API_BASE = 'https://your-backend.onrender.com';
```

### 3. Настройте GitHub Pages:

1. Settings → Pages
2. Source: `main` branch
3. Folder: `/` (root)
4. Сохраните

### 4. Обновите CORS на Render:

В `server/server.js` уже есть `app.use(cors())`, что разрешает запросы с любых доменов.

## ⚠️ Важные замечания

1. **Бесплатный tier:**
   - Сервер "засыпает" после 15 минут неактивности
   - Первый запрос после сна может быть медленным (cold start)
   - Для production лучше использовать платный tier ($7/месяц)

2. **База данных:**
   - SQLite файл хранится в файловой системе
   - При перезапуске сервера данные сохраняются
   - Для production лучше использовать PostgreSQL

3. **HTTPS:**
   - Render автоматически предоставляет HTTPS
   - Сертификат обновляется автоматически

## 🔧 Проверка работы

После деплоя проверьте:

1. **Backend работает:**
   - Откройте `https://your-app.onrender.com`
   - Должна открыться игра

2. **API работает:**
   - `https://your-app.onrender.com/api/stats` (требует авторизации)

3. **Telegram бот:**
   - Откройте бота в Telegram
   - Отправьте `/start`
   - Бот должен ответить

## 📞 Поддержка

Если возникли проблемы:
- Проверьте логи в Render Dashboard
- Убедитесь, что все переменные окружения установлены
- Проверьте, что токен бота правильный

